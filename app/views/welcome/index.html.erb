<div class="jumbotron">
  <% if user_signed_in? %>
    <h1>Welcome back to throw<i>Back</i>!</h1>
    <p class="lead">
      You are already signed in.
    </p>
    <%= link_to "Dashboard", dashboard_index_path, :class => 'btn btn-success' %>
  <% else %>
    <h1>Welcome to throw<i>Back</i>!</h1>
    <p class="lead">
      Sign in or sign up to begin building your throw<i>Back</i> timeline today.    </p>
    <div class="row-fluid">
      <div class="span6">
        <%= link_to "sign in", new_user_session_path, :class => "btn btn-success" %>
           
        <%= link_to "sign up", new_user_registration_path, :class => "btn btn-success" %>
      
    </div>
  <% end %>
</div>
